<h1><%= @snack.name %></h1>
<h4>Price: <%= number_to_currency(@snack.price) %></h4>
<h5>Locations:</h5>
<p><% @snack.machines.each do |machine| %></p>
  <%= machine.location %>
  <p><%= machine.total_snack_count %> kinds of snacks, average price of <%= number_to_currency(machine.average_snack_price) %></p>

<% end %>
